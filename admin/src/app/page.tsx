// This page is intentionally blank.
// The middleware in src/middleware.ts redirects all authenticated users
// to their role-specific page automatically.
export default function RootPage() {
  return null;
}
